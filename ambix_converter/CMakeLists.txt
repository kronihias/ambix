SET ( SPECIFIC_SOURE_DIR ${SRC_DIR}/ambix_converter )

# VST2 fixed-order
IF(BUILD_VST)
	SET(UNIVERSAL_BUILD FALSE)
	SET( PLUGIN_CODE ACo${AMBI_ORDER})
	INCLUDE(${SRC_DIR}/Subproject.cmake)
ENDIF()

# VST3/AU universal
IF(BUILD_VST3 OR BUILD_AU)
	SET(UNIVERSAL_BUILD TRUE)
	SET( PLUGIN_CODE ACoU)
	INCLUDE(${SRC_DIR}/Subproject.cmake)
ENDIF()
